# Inference server deps, on top of what the BlockGen repo already needs
# (torch + numpy). Install into the repo venv:
#     .venv/bin/python -m pip install -r deploy/inference/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0

# Only needed for text-conditioned models (kind: cond_piece_ar), which embed the
# prompt with the same CLIP ViT-B/32 encoder used to build the training conditions.
# The unconditional models do not import this.
transformers>=4.40

# Only needed for the 'agentic' models (kind: agentic), which call an LLM API
# instead of loading a checkpoint. The openai client also drives Gemini through its
# OpenAI-compatible endpoint, so this one package covers both; add anthropic only if
# you point at a claude-* model. Keys come from the repo .env.
openai>=1.40
python-dotenv>=1.0

# Tests
pytest>=8.0
