Related Works
BrickGPT

Problem setting
Most general, we have discrete set we'll call a palette of elements $\mathcal{P}={e_1​,e_2​,\dots,e_n​}$. Each $e\in P$ has some properties $[x_1,\dots,x_m]$. 
For example, in a palette of LEGOs, there may exist an element with properties "1x2", "two male attachments on top", "two on bottom", "red". Maybe separate attachment points, a set of attachment points--we'll start with undirected attachments. We denote the set of real structures built from elements $\mathcal{D}$ where a sample $S\in\mathcal{D}$ where $S=(V,E)$ where vertices/nodes are elements and 

Build a model to sample

Interesting to be able to sample from 

Method

